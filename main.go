package main

func main() {
	// sortings.Bubble()
	// sortings.Insert()
	// sortings.Selection()

	// strings.ReverseString()
	// strings.Palindrome()
	// strings.Vowels()

	// hashtable.Counter()
	// hashtable.Set()

	// b := &binarytable.BST{}
	// b.Insert(10)
	// b.Insert(100)
	// b.Insert(1000)
	// b.Insert(50)

	// binarytable.Inorder(b.Root)
	// fmt.Println()
	// binarytable.PreOrder(b.Root)
	// fmt.Println()
	// binarytable.PostOrder(b.Root)

	// h := &heaptable.MinHeap{}

	// heap.Init(h)

	// heap.Push(h, 40)
	// heap.Push(h, 10)

	// fmt.Println((*h)[0])

	// for h.Len() > 0 {
	// 	fmt.Printf("%d ", heap.Pop(h))
	// }

	// fmt.Println(heap.Pop(h))

	// copyheap := make(heaptable.MaxHeap, len(*h))
	// copy(copyheap, *h)

	// for copyheap.Len() > 0 {
	// 	fmt.Printf("%d ", heap.Pop(&copyheap))
	// }

	// h := &heaptable.MinHeap{}

	// heap.Init(h)

	// heap.Push(h, 50)
	// heap.Push(h, 10)
	// heap.Push(h, 500)
	// heap.Push(h, 30)

	// fmt.Println(heap.Pop(h))

}
